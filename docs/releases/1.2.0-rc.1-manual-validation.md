# Ariana 1.2.0-rc.1 Manual Validation

Use this file to record manual validation for Ariana `1.2.0-rc.1`.

Do not mark the RC ready for stable promotion until every required item is completed.

## Operator

```txt
name:
date:
commit:
```

## Local gates

```bash
git pull origin main
npm install --no-audit --no-fund
npm run release:gates:v1.2
```

Result:

```txt
pending
```

## Candidate workflow

```txt
Actions -> Ariana 1.2 RC Release Candidate
version: 1.2.0-rc.1
run_gates: true
```

Result:

```txt
pending
```

Artifacts to inspect:

```txt
ariana-1.2.0-rc.1-npm-tarballs
ariana-1.2.0-rc.1-manifest
ariana-1.2.0-rc.1-release-notes
```

## NPM dry run workflow

```txt
Actions -> Ariana 1.2 NPM Dry Run
version: 1.2.0-rc.1
dist_tag: next
```

Result:

```txt
pending
```

## Manual UI test: /animate

Open:

```txt
http://localhost:5173/animate
```

Required checks:

- [ ] Hero enter animation is visible.
- [ ] Panel enter animation is visible.
- [ ] Add card works.
- [ ] Remove card works.
- [ ] Add stagger item works.
- [ ] Remove stagger item works.
- [ ] Flow scene works.
- [ ] Data scene works.
- [ ] Security scene works.
- [ ] Slow mode and normal mode both work.

Result:

```txt
pending
```

## Manual UI test: /route-outlet

Open:

```txt
http://localhost:5173/route-outlet
```

Required checks:

- [ ] Mount outlet works.
- [ ] Alpha route renders.
- [ ] Beta route renders.
- [ ] Gamma route renders.
- [ ] Route leave transition runs before replacement.
- [ ] Route enter transition runs after render.
- [ ] Destroy outlet clears the host.
- [ ] Clicking another route after destroy safely recreates the outlet.

Result:

```txt
pending
```

## GitHub draft release

```txt
Actions -> Ariana 1.2 GitHub Release
version: 1.2.0-rc.1
confirm: CREATE_GITHUB_RELEASE
```

Result:

```txt
pending
```

## NPM next release

Only run after approval.

```txt
Actions -> Ariana 1.2 Next Release
version: 1.2.0-rc.1
confirm: RELEASE_NEXT
```

Result:

```txt
pending
```

## NPM registry verification

```txt
Actions -> Ariana 1.2 Verify NPM Release
version: 1.2.0-rc.1
dist_tag: next
```

Result:

```txt
pending
```

## External install smoke

```bash
mkdir ariana-rc-smoke
cd ariana-rc-smoke
npm init -y
npm install @ariana-framework/core@next @ariana-framework/router@next @ariana-framework/vite-plugin@next
```

Result:

```txt
pending
```

## Feedback

```txt
pending
```

## Stable promotion decision

```txt
pending
```
