# Ariana 1.2 Version Task List

This file tracks the release work for Ariana `1.2`.

## Current version

```txt
1.2.0-rc.1
```

## Release strategy

Ariana `1.2` should move in this order:

```txt
1.2.0-rc.1 -> npm next -> feedback -> 1.2.0 stable
```

Do not publish `1.2.0-rc.1` as `latest`.

## RC readiness tasks

- [x] SCSS `styleUrl` support.
- [x] Template animation API.
- [x] Route transition metadata.
- [x] Router outlet demo.
- [x] Release notes.
- [x] Release checklist.
- [x] Candidate workflow.
- [x] NPM dry run workflow.
- [x] Protected npm next workflow.
- [x] GitHub draft release workflow.
- [x] NPM registry verification workflow.
- [x] Workflow hygiene check.
- [x] Packed tarball content verification.
- [x] Packed internal dependency verification.
- [x] Packed install smoke.
- [x] Manifest, checksum and operator summary.

## Manual validation tasks

- [ ] Run local release gates.
- [ ] Run candidate workflow with gates enabled.
- [ ] Run npm dry run workflow with `next` tag.
- [ ] Manually test `/animate`.
- [ ] Manually test `/route-outlet`.
- [ ] Create GitHub draft release.
- [ ] Publish RC to npm `next`.
- [ ] Verify npm registry state.
- [ ] Run external install smoke from npm `next`.
- [ ] Collect feedback before stable promotion.

## Stable promotion tasks

Start these only after the RC has passed manual validation and feedback.

- [ ] Change root version from `1.2.0-rc.1` to `1.2.0`.
- [ ] Create stable release notes for `1.2.0`.
- [ ] Create stable release workflow or reuse protected publish flow with `latest`.
- [ ] Run full release gates.
- [ ] Run external install smoke.
- [ ] Publish `1.2.0` to npm `latest`.
- [ ] Verify npm `latest` dist tag.
- [ ] Publish final GitHub release.

## Rule

The stable version must be a separate release. Do not convert the RC artifact into stable without rebuilding and re-verifying it.
