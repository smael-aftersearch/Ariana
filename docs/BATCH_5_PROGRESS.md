# Batch 5 Progress

Completed:

- query public exports for helper modules
- createLazyRouter API
- createLazyRouter test
- create-ariana smoke validation
- CI create-ariana smoke step
- next public surface note

Also retained previous batch additions:

- bootstrapApplication destroyable reference
- FormArray async validation
- Vite typecheck option groundwork
- Vite component fixture files

Next:

- fix CI failures from stricter checks
- wire compiler typecheck into main compiler export when safe
- add generated app build smoke test
- promote CLI build into root build after tsconfig stabilization
